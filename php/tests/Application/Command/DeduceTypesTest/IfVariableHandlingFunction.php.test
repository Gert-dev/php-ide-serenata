<?php

namespace A;

if (
    is_array($b) ||
    is_bool($b) ||
    is_callable($b) ||
    is_double($b) ||
    is_float($b) ||
    is_int($b) ||
    is_integer($b) ||
    is_long($b) ||
    is_null($b) ||
    is_numeric($b) ||
    is_object($b) ||
    is_real($b) ||
    is_resource($b) ||
    is_scalar($b) ||
    is_string($b)
) {
    // <MARKER>
}
